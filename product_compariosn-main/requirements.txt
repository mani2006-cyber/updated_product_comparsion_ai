torch>=2.1.0
transformers==5.14.1
datasets>=2.19.0
accelerate>=0.30.0
evaluate>=0.4.2
peft>=0.11.0
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
sentencepiece>=0.2.0
protobuf>=4.25.0
safetensors>=0.4.0
fastapi>=0.110.0
uvicorn>=0.29.0

# exactmatch/llm_arbiter.py -- LLM escalation for the hard near-miss slice.
# Needs ANTHROPIC_API_KEY (or an `ant auth login` profile) to actually call
# out; absent that it degrades to the cross-encoder's own verdict, so these
# are safe to install even before credentials exist.
anthropic>=0.122.0
pydantic>=2.0.0